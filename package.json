{
  "name": "knowledge-bits",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "postinstall": "pnpm --filter @knowledge-bits/contracts build && pnpm --filter @knowledge-bits/pipeline build && pnpm --filter @knowledge-bits/api prisma:generate",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "pnpm test:contracts && turbo run test --concurrency=1 && pnpm test:e2e",
    "test:contracts": "node --test test/deployment-contracts.test.mjs test/credential-scanner.test.mjs",
    "test:e2e": "node --test --test-concurrency=1 test/e2e/*.test.mjs",
    "scan:forbidden-credentials": "node scripts/scan-forbidden-credentials.mjs",
    "dev": "turbo run dev",
    "prisma:generate": "pnpm --filter @knowledge-bits/api prisma:generate",
    "migration:registry:validate": "node scripts/validate-nuglet-migration-registry.mjs",
    "migration:registry:verify-live": "node scripts/validate-nuglet-migration-registry.mjs --live"
  },
  "devDependencies": {
    "turbo": "2.9.18",
    "typescript": "6.0.3"
  }
}
